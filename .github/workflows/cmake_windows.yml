name: windows

on: [push]

jobs:
  build:

    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v1
      - name: build
        run: |
          echo dir
          dir

          echo ls
          ls