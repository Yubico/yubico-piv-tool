name: CMake Build Windows

on: [push]

jobs:
  build:

    runs-on: windows-latest

    steps:
      - uses: actions/checkout@v1
      - name: build
        run: |
          echo Get-ChildItem env:
          Get-ChildItem env:
